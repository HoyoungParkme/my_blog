---
title: 집계 데이터 필드명 불일치로 AI 응답이 어긋나던 문제
date: 2026-02-01
type: 트러블슈팅
tags: [BI, Tableau, AI, Architecture]
summary: BI 도구의 집계 데이터와 AI가 참조하는 필드명이 어긋나면서 생긴 문제를 정리하고, 통합 아키텍처의 기준을 세운 과정을 기록합니다.
takeaway: BI 도구는 집계 결과에 자체 규칙으로 필드명을 붙인다. AI 쪽 스키마와 이름을 맞추는 계층을 두지 않으면 조회는 성공하는데 값만 틀린 상태가 된다.
---

## 문제 상황

AI가 대시보드 데이터를 참조해 답변을 만드는 구간에서, 질의는 정상 처리되는데 값이 엉뚱하게 나오는 경우가 있었습니다. 조회 자체가 실패한 것이 아니라서 에러로 잡히지 않았습니다.

> TODO: 어긋난 값이 나온 실제 질의와 응답 예시 추가.

## 원인 분석

BI 도구가 집계를 수행하면서 필드명을 자체 규칙으로 바꿔 내보내고 있었고, AI 쪽이 참조하는 스키마는 원본 필드명 기준이었습니다. 이름이 다르니 다른 필드를 읽었고, 형태가 맞는 값이 나왔기 때문에 아무도 실패로 인지하지 못했습니다.

이름이 어긋나 있어도 파이프라인이 끝까지 도는 구조라는 것이 문제였습니다.

> TODO: 실제로 어긋났던 필드명 대응 관계 예시 추가.

## 해결과 검증

집계 결과의 필드명과 AI가 참조하는 스키마를 잇는 기준을 명시적으로 정리했습니다. 매핑을 코드 여기저기에 흩어두지 않고 한곳에서 관리해, 필드가 늘어날 때 대응 관계를 함께 갱신하도록 했습니다.

BI와 AI를 붙이는 작업에서 반복해서 나오는 문제라, 이후 통합 아키텍처를 설계할 때의 기준으로 삼았습니다.

> TODO: 매핑 계층 적용 후 검증 방법과 결과 기록.
